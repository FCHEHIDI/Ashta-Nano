# Context/notes files (root-level markdown — not part of the codebase)
/*.md

# Rust build artifacts
/target/
crates/*/target/

# Cargo lock (keep for binaries, ignore for libs — we keep it for reproducibility)
# Cargo.lock  ← intentionally tracked

# Binance / market data (can be large, re-downloadable)
data/*.csv
data/*.zip
data/*.bin
data/*.alog

# Test artifacts generated by cargo test
/tmp/ashta_*

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
